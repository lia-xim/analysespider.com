<!-- token-efficient-contract:v1 -->
## Efficient agent workflow

Use `$work-token-efficiently` for repository tasks. Load only task-relevant files and references, prefer targeted searches and focused verification, and never trade correctness, safety, rights, or required proof for token savings.

# Agent contract for analysespider.com

Before planning, writing, redesigning, publishing, or materially changing this site:

1. Read `README.md`, `PROJECT_BRIEF.md`, and `src/data/site.ts` completely.
2. Validate `C:\Users\matth\Documents\DomainPortfolio` with the installed manager scripts and read `strategy/domains/analysespider.com.md` plus `strategy/projects/contextter.md`.
3. Read the current owned-domain rebuild handoff at `C:\Users\matth\Documents\DomainPortfolio\reports\owned-domain-rebuild\domains\analysespider.com.md` before changing a legacy URL action.
4. Treat the recorded purpose, identity disclosure, rights constraints, linking policy, operating burden, and stop condition as hard context.
5. Treat `src/data/rights-evidence.json` and `src/data/legacy-url-actions.json` as versioned implementation contracts.
6. Do not copy former-site text, code, media, downloads, authorship, users, customers, subscribers, or identity without documented rights.
7. Do not present a commonly owned portfolio site as independent corroboration.
8. Keep `noindex` until the real site has explicit launch approval and verified production evidence.
9. Do not enable unrestricted server-side fetching, active security scanning, port scanning, or unrestricted crawling.
10. After a material implementation or deployment change, update the canonical DomainPortfolio with the manager scripts, render its views, and validate both stores.

No commit, deployment, DNS change, or launch-state change is implied by an implementation request.
