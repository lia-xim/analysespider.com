---
import type { Locale } from "../data/i18n";

interface Props {
  current: string;
  locale?: Locale;
}

const { current, locale = "en" } = Astro.props;
const de = locale === "de";
const tools = [
  { href: de ? "/de/tools/crawler-sicht" : "/tools/crawler-view", label: de ? "URL-Check" : "URL check" },
  { href: de ? "/de/tools/google-index-check" : "/tools/google-index-check", label: de ? "Google Index" : "Google index" },
  { href: de ? "/de/tools/weiterleitungskette" : "/tools/redirect-chain", label: "Redirects" },
  { href: de ? "/de/tools/http-antwort" : "/tools/url-inspector", label: "HTTP" },
  { href: de ? "/de/tools/robots-regel-test" : "/tools/robots-rule-tester", label: "robots.txt" },
  { href: de ? "/de/tools/server-log-analyse" : "/tools/log-file-inspector", label: de ? "Server-Logs" : "Server logs" },
  { href: de ? "/de/tools/ip-adresse" : "/tools/ip-location", label: de ? "IP prüfen" : "IP check" },
  { href: de ? "/de/tools/bot-verifizierung" : "/tools/bot-verification", label: de ? "Bot verifizieren" : "Verify bot" },
];
---

<nav class="tool-navigator" aria-label={de ? "Zwischen Werkzeugen wechseln" : "Switch between tools"}>
  <a class="tool-navigator-all" href={de ? "/de/tools" : "/tools"}>{de ? "Alle Tools" : "All tools"}</a>
  <div>
    {tools.map((tool) => (
      <a href={tool.href} aria-current={current === tool.href ? "page" : undefined}>{tool.label}</a>
    ))}
  </div>
</nav>
