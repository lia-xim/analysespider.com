---
interface Props {
  title: string;
  intro: string;
  index?: string;
}
const { title, intro } = Astro.props;
---
<header class="page-lead">
  <div>
    <h1>{title}</h1>
    <p>{intro}</p>
  </div>
</header>
